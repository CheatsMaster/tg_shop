{
  "buildCommand": "mkdir -p .vercel/output/static && echo 'Crash Shop API' > .vercel/output/static/index.html",
  "outputDirectory": ".vercel/output/static",
  "devCommand": "echo 'Starting dev server...'",
  "installCommand": "echo 'No install needed'"
}